Hermes Agent has been installed.

Service:
  {{ include "hermes-agent.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

The runtime manager listens on {{ .Values.runtimeManager.host }}:{{ .Values.runtimeManager.port }} inside the pod.

The API key is stored in:
  Secret: {{ include "hermes-agent.secretName" . }}
  Key: {{ .Values.secret.key }}

The shared Hermes data root is mounted at:
  {{ .Values.hermesHome.mountPath }}

Per-user Hermes homes are resolved under:
  {{ .Values.runtimeManager.usersRoot }}
